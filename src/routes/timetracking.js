'use strict';
// Time tracking is handled inline in tickets.js for the clock in/out endpoints
// This module exports nothing but is kept for future standalone time reports
module.exports = require('express').Router();
